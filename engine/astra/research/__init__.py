"""Research evidence package: record schemas, splits, stats, cassettes.

Binds every reported metric to a dataset manifest, a label set, a split, and
a benchmark definition (docs/BENCHMARKS.md), so a number in a report can
always be traced back to the evidence that produced it. See
`research/README.md` in the repo root for the artefact-tree storage policy.
"""
